export const metadata = {
  title: 'Blog — MomzVeda',
  description: 'Parenting tips, recipes, and real talk for moms at every stage.',
  alternates: { canonical: '/blog' },
};

export default function BlogLayout({ children }) {
  return children;
}
